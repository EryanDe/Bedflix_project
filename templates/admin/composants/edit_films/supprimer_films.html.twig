<form method="post" action="{{ path('app_chapitres_edit_delete', {'id': chapitre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapitre.id) }}">
    <button class="btn">Delete</button>
</form>
